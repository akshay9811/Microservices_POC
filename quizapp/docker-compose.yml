services:
  mysql:
    image: mysql:8.4
    container_name: quizapp-mysql
    restart: unless-stopped
    command: --performance-schema=OFF --innodb-buffer-pool-size=128M --max-connections=50
    environment:
      MYSQL_ROOT_PASSWORD: Ruchi@123
      MYSQL_DATABASE: quiz_app
    ports:
      - "3306:3306"
    volumes:
      - quizapp_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pRuchi@123"]
      interval: 10s
      timeout: 5s
      retries: 10
    mem_limit: 512m

  quizapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quizapp-api
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/quiz_app
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: Ruchi@123
      JAVA_TOOL_OPTIONS: -XX:MaxRAMPercentage=75 -XX:InitialRAMPercentage=25
    ports:
      - "8080:8080"
    mem_limit: 384m

volumes:
  quizapp_mysql_data:
